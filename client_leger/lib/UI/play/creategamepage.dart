import 'package:flutter/material.dart';

class GameCreationPage extends StatelessWidget {
  const GameCreationPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Game Creation Page'),
    );
  }
}
