import 'package:flutter/material.dart';

class OrganizerGamePage extends StatelessWidget {
  const OrganizerGamePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
        child: Column(
      mainAxisAlignment: MainAxisAlignment.center,
      children: <Widget>[
        Text(
          'Organizer Game Page',
        ),
      ],
    ));
  }
}
