import 'package:flutter/material.dart';

class EquippedPage extends StatelessWidget {
  const EquippedPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Equipped Page Content'),
    );
  }
}
